#include "scene.hpp"

namespace robinglut
{
	/**
	 * Creates a new scene.
         */
	scene::scene()
	{

	}

	/**
	 * Frees all used resources.
         */
	scene::~scene()
	{

	}
	
	/**
	 * Display method, called when the screen is repainted.
	 * 
	 * Does nothing.
         */
	void scene::display()
	{
		
	}
	
	/**
	 * Reshape method, called when the window is resized.
	 * 
	 * Does nothing.
	 * 
         * @param width New width.
         * @param height New height.
         */
	void scene::reshape(int width, int height)
	{
		
	}
	
	/**
	 * Keypress handler, called when a key is pressed.
	 * 
	 * Does nothing.
	 * 
         * @param key Key pressed.
         * @param x ?
         * @param y ?
         */
	void scene::on_keypress(unsigned char key, int x, int y)
	{
		
	}
	
	/**
	 * Mouse click/release handler, called when a mouse button is pressed/
	 * released.
	 * 
	 * Does nothing.
	 * 
         * @param button Which button is pressed/released.
         * @param state The state (pressed/released).
         * @param x Current X position of the mouse pointer.
         * @param y Current Y position of the mouse pointer.
         */
	void scene::on_mouse(int button, int state, int x, int y)
	{
		
	}
	
	/**
	 * Mouse movement handler, called when the mouse is moved while a button
	 * is clicked.
	 * 
         * @param x Current X position.
         * @param y CUrrent Y position.
         */
	void scene::on_active_motion(int x, int y)
	{
		
	}
	
}
